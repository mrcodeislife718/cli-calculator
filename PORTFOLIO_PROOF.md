# CLI Calculator — Portfolio Proof Contract

**Track:** Engineering proof

Completion requires correct arithmetic behavior, deterministic CLI handling, edge-case/error coverage, and reproducible execution.

Required proof: unit tests for supported operations; invalid input, divide-by-zero, numeric-boundary, and parsing cases; CLI integration tests; simple latency/startup baseline where useful; documented setup and limitations.

**Next proof target:** create a compact conformance suite covering every supported operation plus invalid and boundary inputs, and run it automatically on each change.